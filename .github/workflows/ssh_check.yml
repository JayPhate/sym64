name: SSH Check

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check key lines
        run: | 
           echo "${{ secrets.SERVER_SSH_KEY }}" | head -3
